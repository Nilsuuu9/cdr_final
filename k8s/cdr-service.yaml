apiVersion: v1
kind: Service
metadata:
  name: cdr-process-service
  namespace: cdr-system
spec:
  selector:
    app: cdr-process-service
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
